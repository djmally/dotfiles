export GOPATH=$HOME/Documents/go
PATH=$PATH:$GOPATH/bin

alias vim='mvim -v'
alias ruby='ruby -w'
alias ls='ls -G'

export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
export PATH="$PATH:$HOME/Documents/edmodo/repos/deliver" # Add deliver to PATH

PROMPT='%n@%m%# '
